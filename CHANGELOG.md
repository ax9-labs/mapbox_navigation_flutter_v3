## 0.0.1

Initial scaffold. See README.md "Status" section for exactly what's
implemented vs. pending — short version: Dart API is real and tested;
Android native implementation is written but not yet compiled against the
real Mapbox SDK (blocked on a `MAPBOX_DOWNLOADS_TOKEN`); iOS is not yet
implemented.
